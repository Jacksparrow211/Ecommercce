import 'package:flutter/material.dart';

class FeedSubscreen extends StatelessWidget {
  const FeedSubscreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
